<div class="container">
  <%= simple_form_for([@list, @bookmark]) do |f| %>
    <%= f.input :movie, collection: Movie.all, input_html: { class: 'select2', multiple: true }, include_hidden: false %>
    <%= f.submit %>
  <% end %>
</div>
